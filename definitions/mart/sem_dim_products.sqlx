config {
  type: "table",
  schema: "semantic_mart",
  tags: ["mart", "dimension"],
  description: "상품 차원. 상품 1건 = 1행. semantic layer가 참조하는 유일한 상품 원천",
  assertions: {
    uniqueKey: ["product_id"],
    nonNull:   ["product_id"],
  },
  bigquery: { clusterBy: ["department", "category"] },
  columns: {
    product_id:  "상품 식별자. PK",
    category:    "상품 카테고리. 지표 전반의 표준 분류 축",
    brand:       "브랜드. 소스가 비어 있으면 '(unknown)'. 증분 MERGE 키라 NULL 을 둘 수 없다",
    department:  "부문",
    unit_cost:   "단위 원가",
    retail_price:"정가",
  },
}

-- DW 컬럼명을 semantic layer 규칙에 맞춘다. 이름 변경은 이 레이어의 일이고,
-- 상류가 category_name 을 바꿔도 충격이 여기서 멈춘다.
-- brand 는 소스에서 24개 상품이 비어 있다 (2026-09-13 측정. 결함 8).
-- NULL 로 두면 daily_ 의 증분 MERGE 키에서 = 매칭이 실패해 재실행마다 행이 쌓인다.
-- 라벨을 붙여 버킷을 드러내는 쪽이 Kimball 의 unknown member 이기도 하다 (P6-1).
SELECT
  product_id,
  product_name,
  COALESCE(brand_name, '(unknown)') AS brand,
  category_name    AS category,
  department_name  AS department,
  sku,
  unit_cost,
  retail_price,
  list_margin_rate,
  distribution_center_id,
  distribution_center_name
FROM ${ref("dim_products")}
